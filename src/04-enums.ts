//Numeric enum
enum Direction {
  Up,
  Down,
  Left = 2,
  Right,
}

//String enum
enum Color {
  Red = "RED",
  Green = "GREEN",
  Blue = "BLUE",
}

//Campuran Number dan String
enum Status {
  Active = "ACTIVE",
  Inactive = 0,
  Pending = 1,
}
